import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'serdar_erciyas_prog3_final',
  webDir: 'www'
};

export default config;
